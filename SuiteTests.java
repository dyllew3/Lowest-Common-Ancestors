import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({
        TestDAG.class, //test case 1
        TestBST.class     //test case 2
})
public class SuiteTests {
	//normally, this is an empty class
}
